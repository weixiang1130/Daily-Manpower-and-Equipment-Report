# Changelog

版本異動摘要。完整背景與設計決策請見 [`docs/milestones/`](docs/milestones/README.md)。

## [節點 32 補強] 2026-08-12 — 機具使用時數不再預填 8 小時（v22.9）
- **會算錯錢**：回報時勾選機具會自動填 8 小時，而總計欄位唯讀（逐台加總）——使用者看到的就是「一勾就鎖 8 小時」。機具綁時租品項時計價是「實際使用時數 × 單價」，申請 4 小時實際只用 1 小時的單子，沒人特地去改就以 8 小時進計價
- 改為**不預填**，欄位留白顯示「請填寫」；**空白與 0 分開處理**——空白＝還沒填（擋下送出並點名哪幾台），0＝確實填了 0（沿用既有異常警告可確認送出）。改版前兩者都被 `parseFloat("")||0` 收斂成 0
- 新檢查必須排在既有「時數為 0」那道之前，否則單台沒填時會先被攔下並叫使用者去勾「0 使用確認」——錯的指引
- 未改成預填「預定使用時數」：那會重蹈覆轍（照樣接受預設值），且預定時數是整張單的總時數而非每台
- 正式資料實查：已到場的機具明細中剛好 8 小時的佔 20%，最多是 11 小時（23%），分布 4～19 小時——多數有改，但五分之一卡在預設值，**建議回頭抽查那 21 筆**
- 驗證：以地端 API 供應真實資料在瀏覽器實際操作，涵蓋勾選／填寫／清空／取消再勾／單台與多台空白攔截／明確填 0／舊單重開原值保留；全程攔截 fetch 未寫入資料庫

## [節點 35] 2026-08-11 — 地端 .NET 8 API 階段 A～D（合約全端點＋權限機制）
- **先補 v22.8 的 SQL 缺口**：節點 34 改了合約卻沒改 DDL，`rateItem`/`rateOtItem`、各站 `rateBindings`、費率書三樣在 SQL 都沒有落點，照原樣移轉會**靜默消失**。已補 2 欄＋3 表，**11 表 → 14 表 5 VIEW**，遷移工具同步
- **`backend/onprem/dotnet/`**：.NET 8 Minimal API 實作 `GET ?scope=all`／`GET ?site=`＋同服務供應靜態前端（同源、免 CORS）。不用 EF、直接 ADO.NET；日期一律本地字串（計價紅線 2）
- **未實作的操作一律回 501**——前端 await-first，默默回 200 會讓使用者填的資料直接消失
- 驗證：與雲端 `scope=all` 逐欄比對 996 筆單據＋12 站 config，**真實差異 0 處**；端到端開站 12 工地／683 點工／313 機具全數渲染，計價彙總（328 單／1,002 工／前2h 1,704／第3h起 2,374）與 SQL 直查完全一致
- **階段 B 寫入端**：`op:record`（含 409）、`master`／`config`／`addOption`／`deleteRecord`
- **409 靠資料庫的鎖**：版本比對與覆寫包在同一交易，取版本時 `UPDLOCK, HOLDLOCK`。先讀後寫會讓兩人同時送出時**雙雙成功**，先送出者的內容無聲消失——12 執行緒並發實測：恰好 1 人成功、11 人 409、`v` 不跳號、無殘缺子層
- **附件描述資料不隨紀錄砍掉重建**：`file_path` 是切換日搬運腳本補填的，抹掉就找不到實體檔案。改為刪除前快照、重建時帶回
- 驗證 105 項全過（往返保真 81＋並發 6＋其餘 18）；階段 A 全量比對重跑無變化
- **階段 C**：附件上傳/下載/刪除、行情通報費率書、clearSite/clearAll——**合約的每一個端點都已實作**，只剩階段 D 的 SSO／權限
- **附件本體放檔案系統**（`ATTACH_DIR`），`file_path` 存相對檔名而非絕對路徑（目錄搬家只改環境變數）；`deleteAttachment` 只憑 id 刪檔不查表——前端是「先存單再刪附件」，要查表才刪會留下永久孤兒檔
- **clearAll 範圍對齊雲端**：雲端是整個 store 全刪（含工地清單、各站 config、費率書），原本只刪紀錄與附件的話兩邊行為會不一致
- 驗證：階段 C 63 項＋clearAll 另建拋棄式資料庫 14 項，全過
- ☠ 事故：階段 B 一段「斷言 op 尚未實作」的測試，在階段 C 實作 clearAll 後語意自己反轉，把本機測試庫清空。雲端正式資料未受影響，已照交付包程序完整重建並對帳一致。破壞性 op 自此只在拋棄式資料庫測
- **階段 D 權限機制**：認證改**內網 Windows 整合驗證**（Scott 裁示：只在內網、只認 AD 帳密）——原本「規格書直接信前端送的 AD 帳號、任何人可冒名登入」這個唯一的真阻礙**不再存在**，也不需要 SSO 端點／apiKey／前端 `/auth` 路由（前端維持零修改）
- **刻意不引 Negotiate 套件**（8.0 線含最新版仍帶兩則高嚴重性弱點），驗證交給主機層 IIS／HTTP.sys；專案目前無任何已知弱點相依
- **權限預設 Off**：不設定就完全維持現行行為，部署新版不會突然把人擋在外面
- API 過濾（`master.sites` 與 `stores` 同步、越權 403）、稽核資料真隔離（v13 只是前端隱藏）、破壞性 op 限管理者（**解掉 clearAll 的安審遺留**）、操作軌跡 log、ERP 斷線 fail-closed
- 驗證 27 項全過（AUTH-PLAN §6 情境 1～6、8～11 ＋ 斷線降級）；`Mode=Off` 回歸重跑階段 A～C 全數通過
- ⚠ 未決：AD 帳號→工號的取得方式（已做成可抽換介面）；Windows 驗證與冒名登入情境**需在網域環境複驗**；`project_code` 正式對映尚未填入

## [節點 34 補強] 2026-08-11 — code review 修正 11 項（v22.8.1）
- **會算錯錢的 4 項**：機具一律用「天數×單價」而不看計價單位（時租單被算成 1/8、月租被算成天數倍）；單價空白/文字被收斂成 0 卻照樣收下並印出金額 0；租工品項未載明加班費率時以 0 元計價卻算成功；生效日取到被濾掉的離群舊日期，會靜默蓋掉上一季
- **會毀掉資料的 3 項**：儲存綁定時整包重建，未渲染的區塊等同被清空；綁定介面不隨切換工地重繪，按儲存會把 A 站綁定寫進 B 站；已存的計價品項若不在本季會靜默掉回「不計價」，重存即永久遺失
- **其他 4 項**：費率載入失敗後永不重試；計價組成算了卻沒顯示（違反計價紅線 4，已新增「計價組成」欄）；必要欄位檢查看第一筆資料列而非表頭；費率書單一 blob 的無防護讀改寫，同時匯入兩種會蓋掉一份（拆成 `rates:labor`／`rates:equipment`）
- 驗證 79 → **93 項全過**（前端 40＋op 邏輯 28＋整份 api.mjs 23）

## [節點 34] 2026-08-10 — 行情通報單價帶入計價（v22.8）
- **管理員自行匯入按季 xlsx**（設定頁，不經 IT）：純前端解壓解析、零依賴；同生效日重匯即取代，不同季別並存（上限 12 季）
- **報表依出工日回查當季費率算金額**——單據不存金額，換季重匯不會動到歷史單據
- **綁定**：系統用簡稱、行情通報用公司全名，以前綴比對自動建議（實測 97% 單據唯一命中、零歧義）。租工逐「廠商×工種」綁定（同廠商同工種依適用工地有多筆價）；機具只綁廠商，品項與加班費率由工程師回報時各挑一項
- **查不到費率一律顯示原因，絕不退化為 0**（0 會被讀成「免費」）；彙總另設「未能計價單數」欄，算不出的不併入金額。金額附組成說明（計價紅線 4）
- 新增 `GET ?rates=1`、`op:rateBook`、`op:deleteRateBook`（雲端與地端兩份實作）；費率書**刻意不進 `scope=all`**，避免拖慢每個人的開站
- 兩個會算錯錢的坑：Excel 序列日期起點是 1899-12-30（Lotus 1900 閏年錯誤），用錯差兩天就跨季抓錯費率；Excel 存出的 sheet 含 104 萬個空列（解壓後 40MB），需只掃有值儲存格否則瀏覽器卡死
- 驗證 **79 項全過**：前端端到端 26（真實兩份 xlsx、金額可心算）＋後端 op 邏輯 28（cloud/onprem 各 14，行為一致）＋**整份 api.mjs 25**（含路由與 Basic Auth）
- 後端測試不重打程式碼：op 邏輯直接抽出兩份實作的 case 內容執行；整份 api.mjs 則以 **Chrome 當 JS 執行環境**載入（本機無 Node），用真的 `Request` 物件打預設匯出，受測檔現拷自 repo

## [節點 33] 2026-08-10 — 回報日期防呆（v22.7）
- **出工日還沒到不能回報**：回報是「這天實際做了什麼」的紀錄，出工日未到就先回報沒有意義
- **簽單繳回日限定 `出工日 ～ 出工日+20 天`**：早於出工日不可能；晚於 20 天視為逾期不予採計。繳回日**可以是未來日期**（簽單尚未收回先預定），只要在 20 天內
- 兩道防線：開表單時把日期選擇器的 `min`／`max` 設成可採計範圍，送出時再驗一次
- `addDays()` 以 `T00:00:00` 解析為本地時間——直接 `new Date("YYYY-MM-DD")` 會被當 UTC，在 UTC+8 退一天讓期限少一天（計價紅線 2）
- **實作前盤點正式資料**：448 筆已回報單中，現有繳回間隔為當日 60 筆、1～7 天 327 筆、超過 20 天僅 1 筆（31 天，看似月份打錯）——門檻寬鬆，不影響正常作業
- 驗證 35 項全過；**點工與機具分開測**（本專案對稱守衛在 v13.1／v18.1 各漏過一個方向）
- 合約新增 §4.7；**目前僅前端強制，地端 API 需在伺服器端重做同樣檢查**

## [節點 32] 2026-08-10 — 機具欄位擴充與計價口徑（v22.6）
- **申請**：新增「預定使用時數」與「申請備註」（包月機具用）；**移除「機具廠商」欄**——工地統一叫車後才配車
- **回報**：新增「機具廠商」「出工天數」「加班時數」「實際工作內容」。加班為**單一欄不分段**，點工的前2h／第3h起規則不適用機具（2026-08-10 裁示）
- **修掉既有缺陷**：差異原本是 `實際使用時數 − 需求數量`＝**拿時數減台數**，從一開始就算不出有意義的值；改為 `實際使用時數 − 預定使用時數`。舊單無預定時數則顯示「未填預定」而非 0（0 會被讀成「相符」）
- **有效廠商唯一權威 `recVendor()`**：廠商從申請層搬到回報層，等於每個讀 `r.vendor` 的地方都可能讀錯——全部 17 處收斂（清單/篩選/報表/彙總分組/稽核選單、表單、紀錄、PDF、CSV）
- **計價彙總**新增「總出工天數」「總加班時數」兩欄（計價紅線 4：要看得見組成），實際使用時數保留供對帳
- 送出前警示新增三條：有時數但天數為 0、天數 > 3、加班 > 12 小時
- SQL DDL 與移轉工具同步；兩個機具 VIEW 改用 `COALESCE(rep.vendor, r.vendor)`；`diff` 放寬為可空
- 驗證 65 項全過，含實際觸發 submit 攔 `fetch` 驗真正送出的 JSON、編輯舊單不洗掉既有廠商與回報

## [節點 31] 2026-08-07 — 說明文字自動列點（v22.5）
- **輸入**：說明欄位打「1.」後按 Enter 自動接「2.」；空編號行按 Enter 結束列點；中間插入會自動重排後續號碼；接受 `1.` `1、` `1)` 三種起手式。**中文輸入法組字中不介入**，否則選字的 Enter 會被吃掉
- **套用 5 個欄位**：現場查核回饋、稽核現場狀況說明、稽核不符原因、點工／機具廠商代辦備註（後三者由 `input` 改 `textarea`）。**設定頁名單池與工作內容補充刻意排除**——前者是「一行一個名稱」，加編號會毀掉名單
- **顯示**：`.fixed-table` 改 `white-space:pre-line`；列點逐點包成區塊做懸掛縮排，折行對齊文字而非跑到編號正下方
- **匯出**：xlsx 新增 `TEXTW`／`PTEXTW` 樣式（含換行的儲存格才套 `wrapText`）——沒有它 Excel 會把整段擠成一行、換行只顯示成小方塊；CSV 既有引號跳脫已足夠
- 資料結構不變，存的仍是純文字，舊單完全相容
- 修掉一個真 bug：`autoGrow` 在面板收合時 `scrollHeight` 為 0，會把欄位壓成高度 0 且不會恢復——改為量不到就不動，並把呼叫移到面板展開之後

## [節點 30 補強] 2026-08-07 — 清單頁一併套用固定欄寬（v22.4）
- **點工清單 2,878px → 1,366px，整頁 2,915px → 1,440px（1440 螢幕完全不需橫拉）**：清單頁有同一個「現場查核回饋」欄，v22.3 只改了報表
- 機具清單、稽核紀錄清單一併套用；總覽跨工地（6 欄純數字）與稽核 PDF（另一份列印 CSS，資料格本來就換行）維持原樣
- **機制抽為共用工具**：`COL_W`（原 `REPORT_COL_W`）＋ `fixedTableOpen()` 產生 `<table>`＋`<colgroup>`＋`<thead>`，**表頭與欄寬同一份來源**；CSS class `.report-table` → `.fixed-table`
- 徽章與按鈕加 nowrap 例外——它們是「一個物件」，字被拆行就散了；操作欄寬依實測訂定（清單 255→260、稽核 202→210）
- 驗證誤判修正：頁籤 id 打錯（`equip`→`equipment`）害 v22.3 那輪「機具頁不變」沒測到；「操作按鈕折行」是假警報（`.row-actions` 是 `<td>` 本身，量到的是整列高度）

## [節點 30] 2026-08-07 — 歷程報表固定欄寬與長字串換行（v22.3）
- **明細表 4,525px → 2,282px，且不再隨資料變動**：改 `table-layout:fixed` ＋ 逐欄指定寬度（`REPORT_COL_W`，以表頭字串為鍵）；先前 `table-layout:auto` 下欄寬由當頁最長那筆決定，翻頁整個版面會跳
- **「現場查核回饋」自動換行**：單欄從 1,748px 收到 220px，128 字折 12 行完整顯示不截斷
- 三個配套：th/td 全部放行換行（fixed 下留 nowrap 會壓到隔壁欄）、表格自帶總寬（width:auto 會讓 Chrome 把 col 的 px 當比例縮放，實測壓成每欄 34px／列高 2,569px）、面板底色與頁首色帶以 `min-width:min-content` 補齊到表格右緣
- 日期欄依實測 118px 訂為 120px（先前 96px 會把 `YYYY-MM-DD` 斷成兩行）
- 純渲染改動：匯出走 `def.rows()`／`pricingSummaryTable()`，不經新表格產生器，計價數字未動

## [節點 28 補強] 2026-08-05 — 閒置守衛修正（v22.2） ※待部署
- **v22.1 的編輯中守衛兩個方向都錯**：`anyEditing()` 只認「編輯既有單」，新開表單填一半不受保護（最常見情境）；而命中守衛時重置計時器，等於表單開著就永遠不會登出——資訊處要的防護完全失效
- 改為 `hasUnsavedInput()` 實際掃描表單內容；寬限不重置計時器；新增 30 分鐘絕對上限
- **sessionStorage 容錯全面套用**：v22.1 只保護登出路徑，其餘 10 處裸呼叫在 Safari 無痕下仍會讓開站卡死；全部收斂到 wrapper
- 小幅整理：`otSegments` 每列重複呼叫改提為區域變數、`|| {}` 統一為 `EMPTY_BAG`

## [節點 29] 2026-08-05 — 目錄重組為前端／後端／文件（v22.1）
- **三大項結構**：`frontend/`（唯一發布目錄）／`backend/{cloud,onprem,sql}/`／`docs/`；README 重寫為導覽入口
- 部署設定同步：netlify.toml 三路徑明示（`[build] edge_functions` 與 `[functions] directory` **寫法不同不可類推**）、build-config 輸出路徑、launch.json、.gitignore
- **code review 修復重組引發的 8 項缺陷**：套件名被誤改導致 Basic Auth 部署失敗、非法的 edge_functions 設定（會讓整站變公開）、地端靜態根指向後端目錄（原始碼可下載）、DATA_DIR 靜默改變、資料目錄未 gitignore（外洩風險）、部署手冊路徑、8 個死連結、去識別化掃描器失效
- **計價正確性**：明細報表的加班歸段改走 `otSegments()`（第四份漏改的副本，舊制單會造成跨表 4 小時幽靈差額）；0 工單帶加班不再從分欄消失
- xlsx 硬化：非數值退回文字格（避免整份檔損毀）、`constructor` 等原型鍵名不再讓匯出崩潰、控制字元改跳脫序列（app.js 不再被當 binary）

## [節點 28] 2026-08-05 — 閒置逾時與登出（v22，資訊處要求） ※待部署
- **閒置 10 分鐘自動登出**：清除本分頁工作狀態與管理員模式、回選站畫面並提示；分頁回前景立即補查（掛過夜回來即登出）
- **頁首新增「登出」按鈕**：confirm 後執行相同流程
- 過渡期限制：Basic Auth 帳密由瀏覽器保存無法程式清除——本機制保護的是工作狀態與管理員模式；SSO 上線後改為丟棄 token，機制沿用

## [節點 27 補強三] 2026-08-03 — MAX 審查 14 項修正（v21.3） ※待部署
- **計價口徑收斂**：加班歸段與逐工種展開改由單一函式（otSegments/reportTypeRows）供彙總、排名、分欄三處共用——杜絕 v19.4 型的副本分歧；分欄小數位統一（兩工作表對帳不再有假差異）
- **彙總表改全前綴表頭**：N 組同名「加班前2HR」會讓樞紐/查表抓錯工種費率；工種計價表維持成本部範本字樣
- 匯出硬化：前導零文字（0800/統編）不再被轉數值、styles.xml count 修正、XML 非法控制字元過濾、iOS Safari 下載時序、檔名消毒、CSV 表頭跳脫
- 結構：移除 v21.1 改路由後的 labor CSV 死分支、匯出一次計算共用快照、名單鍵防 __proto__、下載收尾三處複本合一
- 去識別化：文件與 commit 曾含真實分包商名——已清理並於本機重寫歷史（待下次部署 force-push）；新增動態敏感詞掃描工具

## [節點 27 補強二] 2026-08-03 — 計價表補查核回饋與工程師欄（v21.2）
- 工種計價表尾端加兩欄：**實際工作內容(現場查核回饋)**＋**簽單責任工程師**（同版式）
- 驗證：正式資料全量實跑，逐列對回原始資料零筆不一致

## [節點 27 補強] 2026-08-03 — 點工匯出改帶格式 Excel（v21.1）
- **使用者裁示「不要是 csv 檔」**：點工明細與計價彙總匯出改為帶格式 xlsx——橘底表頭、粉底資料列、民國日期，對照成本部既有計價表版式
- **明細含兩個工作表**：「工種計價表」（施工廠商｜日期｜逐工種 出工數/加班前2HR/加班2HR後）＋「完整明細」（原 38 欄全量備查，數字為真數值）
- xlsx 產生器擴充為多工作表；機具匯出維持 CSV（無工種概念）
- 驗證：正式資料實跑，Excel 實開無警告（FileFormat=51）、兩工作表逐工種合計一致、彙總分欄合計＝原總欄

## [節點 27] 2026-08-03 — 匯出依工種展開分欄（v21）
- **明細與計價彙總 CSV 依「工種」逐一分欄**：每工種三欄（出工數／加班前2h／加班2h後），不同工種費率不同，成本部可直接按欄計價——格式對照使用者提供的計價 Excel
- 欄序依期間內各工種總量降冪；無工種列的舊單以「工作內容類別」為欄名，加班歸段口徑與彙總一致
- 有該工種顯示數字（含 0）、沒有留空白——0 與空白在計價上意義不同
- 既有欄位原封不動（分欄追加於尾端）；畫面顯示不變，僅匯出檔分欄

## [節點 25 補強四] 2026-07-31 — 匯出改真 xlsx（v19.5）
- **修正 Excel 開檔警告**：原匯出為「HTML 表格存成 .xls」，Excel 2007 起會比對副檔名與實際格式，每次開檔都跳「檔案格式與副檔名不相符…可能已損毀或不安全」
- 改為產生**真正的 xlsx**（自建最小 OOXML 產生器，仍零外部套件）：原有版式全部保留（表頭紫底、工種粉底合併、小計米色底、框線、數字格式）
- **數值成為真數值**：不再是套格式的文字，可直接運算與樞紐分析
- 驗證：zipfile CRC 全過、openpyxl 載入合併與樣式皆正確、Excel 實開 FileFormat=51（真 xlsx）

## [節點 25 補強三] 2026-07-31 — 排名報表 code review 修正（v19.4）
- **修正加班分段被壓平（計價紅線）**：未新增工種列即送出的回報單（`workTypes` 空但分段加班有值），排名報表原將整包加班算作「前 2 小時」，跨越費率分界；改用與計價彙總相同口徑——只有「僅有 totalOT」的舊制單才整包歸前 2 小時
- **版面真正固定**：補 `table-layout:fixed`（原先只有 max-width，auto 版面下 colgroup 寬度會被長內容撐破）；工種欄長字串改換行不溢出；欄寬由程式內嵌移到樣式表集中管理
- **小計列補回工種標示**：改用 rowspan 讓工種格跨整組，捲到小計時仍看得出屬於哪個工種
- **排名報表分頁**：以「工種」為單位分頁（每頁 10 個工種），小計不會與所屬資料列分頁；匯出仍為全量
- 欄位定義畫面與匯出共用單一來源，避免改欄位漏改一邊

## [節點 26] 2026-07-31 — 開站動線優化（v20）＋帳號權限規畫
- **選站即開工**：攔截頁選定工地後直接進入「點工申請/回報」頁（不再停總覽再點一次）；總覽仍在頁籤隨時可回
- **單一工地自動進入**：可見工地僅一個時免選站直進——為未來 AD 權限過濾鋪路（過濾後工地使用者開站零點擊直達）
- **過渡期裁示**：權限系統上線前維持自由跳轉（切換器與總覽照常），不做「一站一連結」等前端層級隔離——工地可見性隔離一步到位交給 API 層權限過濾（曾實作 URL 鎖站與專屬連結產生器，依裁示於部署前移除）
- **新增 docs/AUTH-PLAN.md**：AD 整合權限規畫（四角色、AD 群組制 vs 對映表、API 層過濾六要點、推進順序）；MIGRATION-PLAN 環境決策第 2 項連結之
- 順手修正設定頁過時提示（新工地不再帶預設名單）

## [節點 25 補強二] 2026-07-31 — 排名報表對齊修正（v19.3）
- **修正表頭與數字錯位**：數值欄表頭原為左對齊、內容右對齊，視覺完全對不齊——表頭改與內容同為右對齊（五個數值欄右緣差 0px）
- **欄寬固定**：以 colgroup 指定各欄百分比（工種 16／排名 7／工程師 19／本工 12／加班兩欄各 14／總工數 18），不再由內容任意伸縮
- **表格限寬 1080px**：原撐滿 1600px 版面使欄距過鬆、數字散開；1900px 螢幕實測收在 1080px
- 數字改等寬字（tabular-nums），整數位對整數位；小計標籤改靠右並移除重複工種名

## [節點 25 補強] 2026-07-31 — 排名報表分段呈現（v19.2）
- **主管需看加班時數**：排名報表改為四個數值欄——**本工**（工種出工數，可含 0.5）／**加班前2h**（時）／**加班2h後**（時）／**總工數**
- **總工數＝本工＋(加班前2h＋加班2h後)÷8**（工數以 8 小時換算），排名依總工數降冪
- 版面改縱向明細：每位工程師一列（工種、排名、姓名、四個數值），每個工種附**小計列**（人數與各欄合計）——四個數字橫向排不下，縱向也利於後續樞紐分析
- 匯出 Excel 同結構並多一欄「加班合計(時)」：工種欄直向合併、小計列米色底、表頭紫底、數字格式
- 舊制單（v11 前無逐工種明細）：本工取實際出工數、加班取 totalOT 並依合約歸段規則歸入「前2h」段

## [節點 25 修正] 2026-07-31 — 排名口徑修正（v19.1）
- **口徑修正（使用者二次裁示）**：總工數＝實際回報的工種出工數（work，可含 0.5），**移除加班折算**——加班換算屬計價階段，報表只呈現回報原值
- 無逐工種明細的單（v11 前舊制／未加工種列）：列名改顯示該單申請的「工作內容類別」（實際回報內容），不再顯示「（未分工種）」
- 畫面口徑說明同步更新

## [節點 25] 2026-07-31 — 工程師叫工排名報表（v19）
- 歷程報表新增「叫工排名」頁籤：統計各簽單責任工程師叫了什麼工種、工數多少，依工種逐列排名（工種列依總工數降冪）
- **口徑（使用者裁示）**：總工數＝出工數＋加班時數折算（前2h＋第3h起 合計 ÷8，8 小時折 1 工），排名依折算後總數；僅計已回報單
- 期間/廠商篩選連動；期間落在同一個月時期別以民國「YYY.M」標示；v11 前無逐工種明細的舊單以總數歸入「（未分工種）」列不漏計
- **匯出帶格式 Excel（.xls）**：仿既有 Excel 慣用版式——期別欄直向合併、表頭「排名N/工數」雙欄群組、工種與總工數粉底/表頭紫底；零外部套件（HTML 表格格式，Excel 直接開啟）
- 純前端改版：後端/合約/SQL 零修改

## [節點 24 補強] 2026-07-30 — 稽核側 409 守衛與文件全面清理（v18.1）
- **修復 XHIGH 審查發現的 409 繞過**：管理員開著稽核表單時，到點工/機具頁開任一表單會觸發 v18 背景整批刷新，使 saveAudit/deleteAudit 送出時的 baseV 漂移、繞過 409（v13.1 修過問題的鏡像）——bgRefetchVerify 與四個載入 fallback 改為稽核表單編輯中（auditSelectedId）時跳過刷新；兩道守衛互為對稱，CLAUDE.md 記載此不變式
- 文件清理 11 處過時內容：README（localStorage/last-write-wins）、app.js 與 api.mjs 檔頭（v8/舊命名空間）、合約（laborTypes 補預設已移除、版本標頭 v14→v18、§3.3 補 v18 快照紀律）、sql/README（7→9 op）、DEPLOYMENT（DATA_DIR 含附件二進位、server.mjs 附件搬運需 attmeta、兩實作已知差異）、import-backup 檔頭附件警語、MIGRATION-PLAN（機具三表為合成資料驗證之限定語）、CLAUDE.md（六池/workers/頁籤數/v18 適用範圍）
- 補寫節點 23、24 的 milestone 文件與索引（v17/v18 當時未依規範建檔）

## [節點 24] 2026-07-29 — 開表單樂觀渲染與 baseV 快照（v18）
- **問題**：點「填寫回報／編輯申請」會先向雲端重抓整個工地資料才渲染，實測正式站需等 1.1～3.7 秒（單一工地 149KB／212 筆，冷啟更久）
- **改法**：四個載入入口（點工/機具 × 申請/回報）改為**先用快取即時開表單**，再於背景抓最新版校正；快取沒有該筆時才退回等待網路。慢速網路（1.5s）實測開表單由 1500ms+ 降至 **61ms**
- **併發安全（關鍵配套）**：表單開啟當下即**快照 baseV**，送出一律以快照為準（合約 §3.3「baseV＝前端載入當下的版本」）——否則背景刷新會讓版本號漂移、繞過 409 保護。實測：他人更新使快取 v 由 5→8，送出仍以 5 為 baseV 並正確觸發 409
- 背景校正發現該單已被他人更新／刪除時，以 toast 提示並保留使用者已填內容（刪除則收回表單）

## [節點 23 補強] 2026-07-29 — 泛用選項亦由各站自建（v17.1）
- `defaultSiteConfig()` 六個名單池（分包商／工程師／工作地點／工作內容／機具類型／工種）**一律為空**，新建工地零種子名單，全部由該站自行建立
- **移除「工種名單為空時自動補預設值」的 v11 相容邏輯**——此邏輯會在載入時把清空的工種名單還原，導致清空無效；改為僅確保欄位為陣列（舊資料缺鍵不致出錯）
- 配套營運操作：清空 8 個無單據工地的泛用選項；3 個已有單據的工地維持不動（見 docs/ops-log.md）

## [節點 23] 2026-07-29 — 各工地名單自行建置（v17）
- `defaultSiteConfig()` 不再帶入分包商與工程師種子名單（新建工地／還原預設值皆為空），此二者屬各工地自有組織資料，須由該站自行建立——避免 A 站名單出現在 B 站選項
- 工作地點／工作內容／機具類型／工種為泛用詞彙，仍保留預設值，新站不至於無從填起
- 配套營運操作（見 docs/ops-log.md）：主力工地名單保留，其餘 10 站清空並保留既有單據引用名稱

## [節點 22 補強三] 2026-07-29 — 歷程報表明細分頁（v16.3）
- 歷程報表明細表格比照清單改分頁（每頁 10 筆、上/下頁＋頁碼資訊），不再一次列出全部資料需長捲
- 任何條件變更（期間、快捷鍵、廠商、工作內容、工程師、切換點工/機具報表）皆自動回第 1 頁
- **CSV 匯出維持全量**：明細與計價彙總皆取全部符合條件的資料，與畫面停在第幾頁無關（月結計價依據不受影響）——已實測確認
- 計價彙總表維持完整顯示（依廠商彙總，筆數少不需分頁）

## [節點 22 補強二] 2026-07-29 — 排版再緊湊（v16.2）
- 縮短冗長欄位標籤（完整說明改以滑鼠提示保留）：如「需求工數（本次申請的總工數，可含小數）」→「需求工數」、「加班時數－前 2 小時（總計）」→「加班·前2h」——標籤過長正是欄位被撐寬的主因
- 欄寬再縮：一般欄位半版→1/3 版面、數字欄 1/4→1/6（輸入框 190→130px）；欄距 14/18px→10/14px、欄內間距 6→4px、面板留白 18/20→14/16px、輸入框內距 10/12→7/10px
- 成效（1600px 實測）：申請表單「出工日期／分包商／申請人／需求工數」四欄併為同一列；回報表單 7 個數字欄由 3 列壓成 2 列（首列並排 5 個）；回報表單總高 923→745px（−19%）
- 手機版（≤720px）仍為單欄全寬、無橫向溢出

## [節點 22 補強] 2026-07-29 — 移除清單內部捲軸與表單欄位緊湊化（v16.1）
- 清單不再有內部小捲軸：.table-wrap 取消捲動容器（overflow:visible），改為整頁捲動；表頭 sticky 基準改為視窗、偏移 122px（頁首 66＋頁籤 56），捲動時精準黏在頁籤下方不被遮；窄螢幕（≤900px）仍保留橫向捲動
- 表單欄位緊湊化：form-grid 改 12 欄細格線——純數字／日期欄位（16 個＋稽核表單 4 個）佔 1/4 版面且輸入框限寬 190px，文字欄維持半版、說明類欄位維持全寬；回報表單「實際出工數／差異／加班前2h／加班第3h起」四欄由四列壓成同一列
- 手機版（≤720px）全部回到單欄、輸入框解除限寬

## [節點 22] 2026-07-28 — 根基營造品牌風格改版（v16）
- 全站配色改為集團品牌規範：暖中性底（#FAFAF7／#F5F1EC）＋深板岩藍重點色（#2E3F5A），取代原 teal/amber
- 字體改品牌指定：CJK Noto Sans TC、內文 DM Sans、標題 Space Grotesk、數據 DM Mono（含 Microsoft JhengHei 離線 fallback）
- 品牌識別：面板標題加 ▎區塊標記、表頭改暖中性底＋板岩藍字、KPI/頁碼/表格數值改等寬字並對齊
- 落實規範：移除全部漸層（頁首改純色）、圓角一律 ≤4px（尖角）、陰影改暖調、轉場統一 200ms ease 無彈跳
- 稽核 PDF 報告內嵌樣式同步品牌化（板岩藍區塊線、暖中性表頭、成功/差異語意色）
- 純樣式改版：HTML 結構、JS 邏輯、後端/合約/SQL 皆零修改；既有 CSS 變數保留為相容映射，元件無需改寫

## [節點 21 補強三] 2026-07-28 — 版面放寬與每頁 10 筆（v15.3）
- 版面由 1080px 放寬至 1600px：寬螢幕不再兩側留白約四分之一，表格欄位完全攤開、不再橫向捲動
- 清單每頁 20 → 10 筆：一頁清單（篩選列＋表頭＋10 筆＋頁碼列）盡量落在單一畫面內，無內部捲軸
- 手機版不受影響（原本即為全寬）

## [節點 21 補強二] 2026-07-28 — 清單分頁與鎖定表頭（v15.2）
- 三個清單（點工/機具/稽核紀錄）改真分頁：每頁 20 筆、上一頁/下一頁＋頁碼資訊，取代 v15.1「顯示全部」展開（展開後仍是長頁面）；改篩選/切工地自動回第 1 頁
- 表格捲動時鎖定表頭：修正 .table-wrap 讓既有的 th sticky 生效（垂直捲動移入容器、max-height 72vh），所有表格（含歷程報表）通用

## [節點 21 補強] 2026-07-28 — 清單截斷與單一人名源頭防堵（v15.1）
- 點工/機具清單預設只列最近 20 筆（新到舊），底部「顯示全部 N 筆」可展開/收回——長清單不再一次全載畫面
- 「XXX/XXX」多人並列從源頭堵住：人員池「＋新增選項」前後端雙重驗證（含 + / \ 、 , ; : 空白等分隔符一律擋下）、管理員批次貼名單逐行驗證；合約 §3.4 補正準規則
- 既有名單中的多人/錯誤選項與歷史回報另列清單待裁示清理

## [節點 21] 2026-07-28 — 現場回饋三項（v15）
- 回報覆核「簽單責任工程師」限一位代表（多人並列擋下；申請可多人、回報限一人）；歷程報表新增工程師篩選——明細/計價彙總/CSV 連動，可統計各工程師經手叫工總數
- 點工/機具清單加「日期＋廠商」篩選列（含符合筆數與清除），覆核找單免捲頁；切工地自動重置
- 四個申請/回報表單改收合式面板：預設收合、點標題展開、編輯/填寫回報自動展開、送出/取消自動收回
- 純前端改版：後端/合約/SQL 交付包零修改

## [節點 20] 2026-07-27 — 附件：簽單掃描檔與稽核現場照片（v14）
- 點工/機具申請單可夾簽單掃描檔、稽核紀錄可附現場照片（手機直接拍照，圖片前端壓縮至長邊 1600px/JPEG 0.8）；每處上限 5 件、僅圖片＋PDF（4MB）
- 稽核 PDF 報告自動嵌入現場照片；回報頁以唯讀縮圖顯示申請單附件
- 後端（api.mjs 首次修改）：att2: 命名空間存檔案本體、op:uploadAttachment/deleteAttachment、GET ?attachment= 下載；deleteRecord/clearSite 連動清附件
- 架構鐵則：單據 JSON 只存描述資料 attachments[]（合約 §4.6），檔案本體獨立存放——全量載入與 JSON 備份不受影響；**JSON 備份不含檔案本體**，切換日需附件搬運（DEPLOYMENT §4）
- 修復既有 bug：申請表單編輯送出未帶 audits[]，會洗掉該單的稽核紀錄（點工/機具皆修）
- SQL 交付包同步：第 11 表 attachments＋遷移工具附件描述資料（LocalDB 重測）；server.mjs 對等實作

## [節點 19 第二輪硬化] 2026-07-21 — MAX 審查 12 項修復（v13.1）
- **409 保護**：稽核選單/編輯前的整站快取刷新改以 otherFormEditing() 把關——其他頁籤有表單編輯中時跳過刷新，杜絕該表單送出時 baseV 漂移繞過併發保護
- **失效機制一般化**：resetAuditView() 統一遞增請求序號，任何清空稽核畫面的動作（切篩選/切站/登出/儲存收尾）都讓飛行中的選取請求失效，取代逐 handler 補丁
- saveAudit/deleteAudit：await 前快照 kind/store（等待期間切換類型/工地不影響寫回位置）；收尾加序號守衛（不清掉使用者新開的表單）；刪除失敗時保留編輯中表單
- 驗證強化：actualCount 加 isFinite（擋 1e400→Infinity）；auditItems 覆蓋值須為非空陣列；渲染守衛（重繪觸發 reset 時不渲染失效表單）；auditDate 延後初始化（跨午夜分頁不再拿到昨天）；PDF 現場說明多行顯示（pre-wrap）
- SQL 交付包：v_audit_log 不符數改 ISNULL 計算（缺 ok 鍵計入不符而非誤報全數相符）、status_at_audit 加 CHECK 值域、equip_audits 數值精度對齊來源（8,2）、遷移工具驗證 auditor（缺漏跳過計數）並正規化非法狀態快照——LocalDB 重測通過
- 全數以 DEMO 鎖定式測試驗證（含可控延遲重現三種競速情境），console 零錯誤

## [節點 19] 2026-07-20 — 成控現場稽核模組（v13）
- 新頁籤「成控現場稽核」：限管理員（工地端完全隱藏）；選 工地/類型/日期/廠商 連動申請單 → 逐項「相符/不相符」查核（不相符必填原因）＋申請數 vs 現場實點差異
- 稽核紀錄清單（期間篩選）＋單筆編輯（更新取代，保留原稽核日期、標記編輯日）＋刪除＋匯出 PDF 報告（列印視圖，零套件；含稽核人員欄與簽章區）與 CSV
- 查核項目文字可由 config.local.js `auditItems` 覆蓋（去識別化＋免改版增減項目）
- 資料存單據 audits[]（沿用 op:record＋版本檢查）——雲端後端零修改；合約新增 §4.5
- SQL 交付包同步：labor_audits/equip_audits 兩表＋v_audit_log（10 表 5 VIEW）、遷移工具支援 audits；LocalDB 實測（DDL 重建、fixture 匯入、壞資料跳過、JSON 不符項數計算）

## [節點 18] 2026-07-15 — SQL 交付包：資料表設計與遷移工具
- docs/sql/：DB-SCHEMA.sql（8 表＋4 VIEW，SQL Server）＋ backup-json-to-sql.py（備份 JSON→INSERT 遷移工具）＋ README（操作↔SQL 對照、409 寫法、遷移與驗收步驟）
- LocalDB 兩輪實測：正式備份 92 筆匯入零錯誤、對帳一致；合成備份補測機具路徑與各類髒資料防護
- 交付前多代理審查確認 13 項問題並全數硬化：XACT_ABORT/-x/-b 匯入安全、孤兒工地保留（is_active=0）、髒資料跳過＋計數、空陣列/換行/時區保真、total_ot 計算欄、彙總 VIEW 補內容彙集欄、合約同步（totalOT 歸段規則、is_active 關係、白名單正準）

## [節點 17] 2026-07-09 — 移除根基自辦欄位、查核回饋取消字數限制（v12）
- 點工/機具回報表單移除「根基自辦」三欄（未填代辦＝全數自辦）；舊單自辦資料原樣承繼保留，報表欄位不動
- 「現場查核回饋」取消 30 字上限與計數器
- API-CONTRACT.md 同步（selfDone* 轉唯讀承繼、conclusion 不限字數）

## [節點 16] 2026-07-07 — API 合約正式化與後端替換餘裕
- 新增 docs/API-CONTRACT.md：前後端接縫合約 v1（7 操作規格、409 並發語意、欄位字典含版本演進、資料表建議、驗收方式）——未來按 IT 標準重寫資料後端的唯一依據，前端零修改
- app.js API 位址可配置：config.local.js 可設 apiBase 覆寫（預設 /api/data 不變，零迴歸）
- DEPLOYMENT.md 新增「未來改接公司標準後端」章節；變更紀律：動欄位先改合約文件

## [節點 15] 2026-07-07 — 可攜式伺服器與地端部署交付（IT handoff）
- 新增 server/server.mjs：零依賴（Node 18+）可攜式伺服器——同一 API 合約＋靜態服務＋Basic Auth，前端無需修改即可脫離 Netlify 於公司伺服器運行
- 新增 server/import-backup.mjs：管理員完整備份 JSON → 地端資料目錄的遷移工具
- 新增 DEPLOYMENT.md：IT 部署手冊（環境需求、部署步驟、Netlify→地端切換與驗證清單、維運建議、已知限制）
- 未經實機執行測試（開發機無 Node），已通過語法驗證與 API 合約逐案比對；部署前由 IT 於測試機驗證

## [節點 14] 2026-07-07 — 安全補強：CSV 公式注入與 record.id 跳脫（v11.1）
- 匯出 CSV 中和以 = + - @ 開頭的非數字儲存格（前置 '），防 Excel 公式注入；純數字（如差異 -1）不受影響
- 清單按鈕 data-id 改經 esc() 跳脫；API 端 record/deleteRecord 驗證 id 格式（^[A-Za-z0-9_-]{1,64}$），杜絕惡意 id 的儲存型 XSS 與 Blobs key 破壞

## [節點 13] 2026-07-07 — 回報改逐工種、分段加班與彙總期間欄（v11）
- 點工申請移除「預計進場人員名單」（工程師不會知道點工姓名）；需求工數改純手填
- 點工回報改「逐工種」覆核：粗工／技術工／打石工⋯（新名單池，可新增、各工地獨立），逐工種填出工數與加班；總數自動加總唯讀
- 加班分段計價：每列與總計皆拆「前 2 小時」「第 3 小時起」，防呆規則同步分段（前 2 小時上限＝出工數×2）
- 計價彙總（畫面＋CSV）新增「期間」欄；明細 CSV 加班拆 3 欄、明細欄改工種格式（舊單 fallback 逐人）
- 舊單資料完整相容：workers/attendance 原樣保留，舊 totalOT 帶入前 2 小時段

## [節點 12] 2026-07-06 — 回報補人、自辦/代辦結構化與計價彙總（v10）
- 點工回報覆核可「現場補入出工人員」：申請單沒填名單也能逐人記出工與加班（補入者標示「補」、可移除）；有逐人名單時總數欄自動加總並改唯讀
- 「根基自辦」「廠商代辦」拆為 工數＋時數＋備註（點工與機具皆同）；舊資料自動歸入備註顯示，CSV 拆為 6 欄
- 歷程報表新增「廠商／工作內容（機具＝機具類型）」條件篩選，與期間篩選連動
- 新增「計價彙總」：畫面依廠商分組彙總（僅計已回報單），並可獨立匯出計價彙總 CSV，檔名附期間與篩選條件

## [節點 11] 2026-07-03 — 共編保險絲與計價保障（v9）
- 修正回報日期 UTC 時區 bug（早上填單被記成前一天，影響計價月份歸屬）
- 共編保險絲：編輯前自動抓最新、版本衝突 409 偵測、「＋新增選項」伺服器端合併
- 歷程報表期間篩選（起迄日＋本月/上月快捷），CSV 同步套用
- 已回報單非管理員不可刪除；管理員可設「計價鎖定日期」凍結期間單據；一鍵下載完整 JSON 備份

## [節點 10] 2026-07-03 — 共用資料庫與填錯工地防呆（v8）
- 業務資料改存雲端共用資料庫（Netlify Functions + Blobs，/api/data）——所有人即時共編，取代各自的 localStorage
- 寫入採 await-first（雲端成功才清表單），失敗保留輸入內容提示重試
- 防呆三道：開站選工地攔截頁、表單常駐工地徽章、申請送出前工地確認視窗
- 頁首新增「↻ 重新整理」，切換頁籤時自動靜默更新

## [節點 09] 2026-07-02 — Netlify 部署與私密名單注入
- 新增 netlify.toml 與 scripts/build-config.mjs：建置時從環境變數 LOCAL_CONFIG_JS 產生 config.local.js
- 真實名單不進 GitHub；Netlify 整站密碼保護

## [節點 08] 2026-07-02 — 機具父子覆核與管理員模式（v7）
- 機具回報改為父子覆核：逐台勾選到場並填實際使用時數、0 使用確認路徑、防呆警告
- 移除現場清點計時器（10 分鐘倒數）
- 管理員模式：清空資料與批次設定需密碼（config.local.js 的 adminPin）；一般使用者仍可用「＋新增選項」
- 清空按鈕縮小、加「管理員」徽章，降低誤觸

## [節點 07] 2026-07-02 — 父子單覆核與多工地隔離（v6）
- 分包商/申請人/工程師/工作地點/點工人員改為智慧搜尋下拉（模糊搜尋＋「＋新增選項」寫入該工地資料庫）
- 點工申請(父)/回報覆核(子)：回報承繼申請單、逐人勾選到場並填出工數與加班時數、0工確認路徑
- 移除「實際工作時數」；簽單實際出工數支援小數（0.5/1.5工）
- 出工數×加班時數防呆警告（彈窗提醒但不阻擋送出）
- 多工地完全隔離：每工地獨立 localStorage 資料庫、明確 Context Switch、每工地預設分包商名單

## [節點 06] 2026-07-02 — Git / GitHub 版本控管建立
- 建立 git 版控與 GitHub 遠端，`main` 為預設分支，改版一律走 PR 流程
- 程式碼庫預設清單去識別化為通用範例值
- 新增 `config.local.js` 機制：地端真實名單自動載入、永不入版控

## [節點 05] 2026-07-02 — 多工地列控支援
- 工地欄位改為申請表單內選定（移除頁首全域工地選單）
- 匯入 16 個實際列控工地
- 總覽新增跨工地彙總卡片與「各工地列控總覽」表格，可點擊跳轉篩選
- 點工清單、機具清單、歷程報表新增工地篩選器

## [節點 04] 2026-07-02 — Excel 欄位對齊與大頁籤重構
- 點工申請/回報、機具申請/回報改為「大頁籤 + 申請／回報子模組」，兩者為真正獨立的表單
- 回報欄位改為完全對齊既有 Excel 查核表（點工出工列控 / 機具出工列控）
- 移除頁首「目前填報工程師」，改由申請人 / 簽單責任工程師各自於對應表單選擇
- 工作地點、工作內容類別、機具類型改為「單行下拉 + 標籤」選取模式

## [節點 03] 2026-07-02 — 查核獨立性與下拉選單化
- 新增申請時隱藏回報區塊，需點選既有紀錄才能填寫回報
- 稽核人員與申請人相同時跳出提醒
- 分包商、人員等欄位改為下拉選單，支援「＋ 新增選項」
- 頁首新增「目前填報工程師」快速帶入申請人欄位
- 清單新增明確的編輯／刪除按鈕

## [節點 02] 2026-07-01 — 點工申請/回報架構重整
- 頁籤由 8 個精簡為 5 個，移除交接簽單、鐵板盤點模組
- 叫工申報/現場稽查更名為點工申請/回報，機具查核拆為機具申請/回報
- 申請與回報合併為單一紀錄（對齊 Excel 同一列的欄位結構）

## [節點 01] 2026-07-01 — 初版原型建置
- 依會議紀錄與既有查核表設計，建立第一版 8 頁籤稽核 APP 原型
