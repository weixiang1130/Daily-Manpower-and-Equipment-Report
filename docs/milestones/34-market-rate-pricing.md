# 節點 34：行情通報單價帶入計價（v22.8）

## 背景

使用者需求：

> 工地回饋希望可以結合行情通報的單價，然後選擇該廠商如果是我們行情通報的廠商，
> 就可以自動在計價的時候帶入金額，不論是點工還是機具都需要參考這個行情通報 excel，
> 這個是按季更新的，所以應該要有更新或是抽換的機制。考量未來在公司內網上，
> 也要有辦法不透過 IT，管理員就可以透過匯入行情通報表就可以更新租工/機具的單價。

公司按季發布兩份 xlsx（租工 188 項、機具 1,499 項），管理員自行匯入，不經 IT。

## 動工前的可行性盤點

需求假設「選了廠商就自動帶入」，但實測**系統廠商名與行情通報供應商名完全同名者為 0**。
原因不是對不上，是**命名層級不同**：系統用工地自己輸入的簡稱，行情通報用公司全名。
改用前綴比對：

| | 唯一命中 | 涵蓋單據 | 命中多家（歧義） |
|---|---|---|---|
| 點工／租工 | 14／17 家 | **562／579 筆＝97%** | **0** |
| 機具 | 9／11 家 | 110／194 筆＝57%※ | **0** |

※ 機具偏低是因為有一個佔位字被用了 82 筆，扣掉後命中率相當高。
**零歧義**是關鍵——代表前綴比對不會綁錯，只會漏綁。

順帶查到系統內有幾組**同音錯字的廠商名**（會讓同一家的計價被拆成兩筆），已回報使用者。

## 三項裁示（2026-08-10）

| 議題 | 決定 |
|---|---|
| 同廠商同工種多個價格（依適用工地不同） | **每個工地綁定一次** |
| 單價來源 | **依出工日查當季費率**（單據不存金額） |
| 實作範圍 | 一次做完租工與機具 |
| 機具品項對照 | **只綁到廠商層級**，實際品項由工程師回報時從該廠商清單挑 |
| 機具加班費率（在來源檔是獨立的列） | **工程師挑兩次**：主品項一項、加班費率一項 |

## 技術：純前端讀 xlsx

xlsx 就是 ZIP，Excel 存出來一律 DEFLATE，用瀏覽器內建的
`DecompressionStream('deflate-raw')` 解，維持零依賴。

**踩到的坑**：Excel 存出來的檔含**上百萬個自閉合空列**（實測 104 萬個、
sheet1.xml 解壓後 40MB），先用 `<row>` 切塊會建出上百萬個物件、瀏覽器直接卡死。
改成**只掃有值的儲存格**，列號從 `r="C5"` 反推，空列完全不碰：

| | 檔案 | 解壓 | 掃格 | 讀出 |
|---|---|---|---|---|
| 租工 | 2.6 MB | 123 ms | 14 ms | 188 列 |
| 機具 | 2.7 MB | 118 ms | 30 ms | 1,499 列 |

**另一個會算錯錢的坑**：Excel 序列日期的起點是 **1899-12-30** 不是 1900-01-01
（沿用 Lotus 的 1900 閏年錯誤）。用錯會整整差兩天，跨季時就抓到上一季的費率。

## 費率解析

**租工**把費率寫在品項文字裡，且有三種寫法，解析器都要吃：

```
打石工3,000元/工、加班前2hr=499元、第3hr起=623元   ← 分兩段，正好對上系統的前2h／第3h起
打石工2,800元/工、加班466元/HR                      ← 單一費率
打石工2,800元/工、打石工加班466元/HR                ← 前綴帶工種名
```

沒寫「第 3 小時起」者視為不分段（`otOver = ot2`）。188 列中 22 列不是費率列
（如「各工種每工統一加保加價費」），匯入時濾掉。
品項內的每工單價與「單位價格」欄**實測 166/166 完全一致**。

**機具**的加班費是獨立的列，計價單位需從品項文字＋單位欄判定：
`全天 455／加班 392／時租 237／半天 203／月租 150／趟次 62`。

## 資料流

```
管理員匯入 xlsx ─→ 前端解析 ─→ op:rateBook（整季覆蓋）─→ blob "rates"
                                                              │
工地綁定（config.rateBindings）───────────────────────────────┤
                                                              ▼
報表／彙總 ── 依出工日取「生效日 ≤ 出工日」最新一季 ── 計算金額
```

- 費率書**刻意不放進 `scope=all`**：一季 1,500 列、多季累積數 MB，
  塞進開站全量讀取會拖慢每個人。改走獨立端點 `GET ?rates=1`，用到才抓
- 以 `effectiveFrom` 為季別唯一鍵：同生效日重匯即取代（可補修正檔），
  不同生效日並存；上限 12 季（三年），超過丟最舊的
- **綁定鍵用「供應商編號＋說明」**，不可用品項文字——租工的品項把價格寫在裡面，每季都會變

## 計價紅線的落實

**查不到費率一律回 `null` 並附原因，絕不退化成 0**——0 在計價表上會被讀成「免費」，
是最難發現的錯。彙總另設「未能計價單數」欄，算不出的單獨立計數而非併入金額。

涵蓋的情境：未綁定、該出工日無適用季別、本季查無綁定的費率列、未挑品項、
本季查無所選品項、有加班時數但未選加班費率。

金額同時附組成說明（`3工×3000＋前2h 2×499＋3h起 1×623`）——計價紅線 4：
報表不能只給一個算完的數字。
（⚠ 首版只算出組成卻沒顯示，是 code review 抓到後才補上「計價組成」欄，見文末補強第 6 項）

## 驗證：79 項全過 ＋ 正式站實測

**前端端到端 26 項**（用真實的兩份 xlsx）：
- 解析：188→166 可用／略過 22、生效日轉正、分段 91 筆、機具 1,499 列全數分類
- 匯入後的費率列**只保留計價需要的欄位**（聯絡人姓名電話等不進資料庫）
- 金額可心算：租工 `3×3000＋2×499＋1×623 = 10,621`、機具 `2×13000＋3×2000 = 32,000`
- 季別選用：8/5 的單用 Q3、11/20 的單自動改用 Q4（`3×3500＋2×583＋1×583 = 12,249`）
- 六種查無費率情境全部回原因而非 0
- 報表明細、計價彙總的金額欄與「未能計價單數」

**後端 op 邏輯 28 項**（cloud 14 ＋ onprem 14，**兩邊行為完全一致**）：
同生效日取代／不同生效日並存／排序／三種 400 驗證／400 後不動既有資料／
保留上限 12 季且丟最舊／刪除冪等／不影響另一 kind。

> 測試方式：**直接抽出 `api.mjs` 與 `server.mjs` 裡真正要上線的 case 內容執行**，
> 不重打一份——重打只會驗到我自己的理解，驗不到實際會部署的碼。

**整份 api.mjs 25 項**（含路由與驗證）：

本機沒有 Node，原本以為新端點只能等上線才驗得到。後來改用 **Chrome 當 JS 執行環境**：
把整份 `api.mjs` 以 ES module 載入（`@netlify/blobs` 用記憶體墊片、`process`／`Buffer`
補最小墊片），再用**真的 `Request` 物件**打它的預設匯出。受測檔在測試前現拷自 repo，
與要部署的位元組完全相同。

涵蓋：`GET ?rates=1` 空結構與 content-type、匯入後讀得回整包 rows、同生效日取代、
兩個 kind 獨立、四種 400、400 後不動既有資料、保留上限、刪除冪等、未知 op、
既有 `op:master`／`scope=all` 不受影響、**費率書確實沒混進 `scope=all`**、
以及設密碼後未授權 401／正確授權 200／密碼錯的匯入 401。

**正式站實測（2026-08-11，部署後）**：以拋棄式生效日 `1990-01-01` ＋ 1,500 列合成資料
（312 KB，≈ 一季機具的量）打真實的 Netlify Functions ＋ Blobs：

| 項目 | 結果 |
|---|---|
| 寫入 | 1.5 秒 |
| 寫完立刻讀回（strong consistency） | 1.4 秒，讀得到 |
| 1,500 列往返完整、數值未失真 | 首列 1001／末列 2500 皆正確 |
| `op:deleteRateBook` 清除 | 已清空，未在正式資料留痕 |

`GET ?rates=1` 回 200 ＋ `application/json`；`scope=all` 頂層仍只有 `master`／`stores`
（494 KB），確認費率書沒有混進去。

## 已知限制

- 綁定介面只列「本工地實際用過的廠商×工種」，沒用過的不會出現（避免上百筆空綁）
- 機具品項下拉會列出該廠商全部品項（多的有數十項），工程師需自行辨識——
  這是「綁廠商而非綁品項」的取捨

---

# 補強：code review 修正 11 項（v22.8.1）

`/code-review HIGH` 對本節點的 489 行新程式抓出 11 項，**全數屬實**，已全部修正。
其中 4 項會直接算錯錢、2 項會毀掉已存的資料。

## 會算錯錢的

| # | 問題 | 修正 |
|---|---|---|
| 1 | `equipAmount` **一律 `天數×單價`**，但 `chargeType` 有 全天／半天／時租／月租／趟次。時租單（500元/HR、8 小時）被算成 500；月租單被算成 `天數×月租` | 依 `chargeType` 決定數量：`全天`＝出工天數、`時租`＝實際使用時數；`半天`／`月租`／`趟次` **系統沒有對應數量欄位**（幾個半天／幾個月／幾趟），一律擋下並說明，不硬乘 |
| 2 | `rateNum` 把空白與文字都收斂成 `0`，該列照樣收下 → 報表印出金額 **0**，且不計入「未能計價單數」 | 匯入時就濾掉 `price <= 0` 的列。這正是本模組明文禁止的「0 讀成免費」 |
| 9 | 品項沒寫加班費率時 `ot2/otOver` 為 0，有加班時數的單**用 0 元計價卻算成功** | 新增 `otParsed` 區分「沒寫」與「真的是 0」；沒寫又有加班時數 → 擋下 |
| 8 | `effectiveFrom` 取**全部原始列**（含被濾掉的非費率列）的最小日期。一個離群舊日期就把整季往前挪——而它又是伺服器端的季別唯一鍵，會靜默蓋掉上一季 | 改取**留用列**裡的眾數 |

## 會毀掉資料的

| # | 問題 | 修正 |
|---|---|---|
| 3 | `saveRateBindings` 整包重建 `rateBindings`。本季沒有租工費率書時整個租工區塊不會渲染，此時儲存機具綁定 → **租工綁定全部消失**。另外選項清單只列前綴建議的那家，手動綁到別家的會顯示「未綁定」並在下次儲存被清掉 | 改為以既有綁定為底、只更新畫面上真的出現的鍵；選項清單一定包含目前已綁定的那家 |
| 4 | `renderRateBindings` 只在 `initRatesPanel` 呼叫，而它在 `boot()` 之前跑（`cur()` 還是 undefined），畫面永遠停在「請先匯入」；切換工地也不重畫 → **按儲存會把 A 站的綁定寫進 B 站** | 拆出 `renderRatesPanel()` 由 `renderAll()` 呼叫（含切站）；`saveRateBindings` 另外快照工地名，避免 await 期間切站寫錯 |
| 10 | 已存的 `rateItem` 若不在本季（換季改名），下拉靜默掉回「（不計價）」，**重開回報單按儲存就永久遺失** | 保留成標示「⚠ 本季查無此品項」的選項，由人決定 |

## 其他

| # | 問題 | 修正 |
|---|---|---|
| 5 | `renderReport` 先把空的 `RATES` 當佔位再 await，載入失敗後永遠不重試——所有金額變「無適用季別」、彙總靜靜印 0 | 改用獨立的 `ratesLoading` 旗標；失敗時把 `RATES` 歸回 `null` 以便重試 |
| 6 | `detail`（計價組成）算了但**沒有任何地方顯示**，`amountCell` 只印數字。文件卻宣稱有附組成——違反計價紅線 4 | 明細新增「**計價組成**」欄，印出 `3工×3000＋前2h 2×499＋3h起 1×623` |
| 7 | 必要欄位檢查看 `rawRows[0]`（第一筆**資料列**）。空儲存格不會出現在資料列裡，第一筆剛好某欄空白就誤判「檔案格式不符」 | `xlsxRows` 改為回傳 `{headers, rows}`，用表頭檢查 |
| 11 | `rateBook`/`deleteRateBook` 對單一 `rates` blob 做無防護的讀改寫（兩種 kind 同放一把），同時匯入租工與機具會**無聲蓋掉一份** | 拆成 `rates:labor` / `rates:equipment` **一個 kind 一把 key**，跨 kind 不再競爭 |

## 驗證：93 項全過

前端 40（新增 G 組 13 項專測上述修正）＋ op 邏輯 28（cloud/onprem 各 14）＋
整份 `api.mjs` 23（新增「同時匯入兩種 kind 不互相覆蓋」）。

新增的關鍵驗算：時租 `8 小時×500 = 4,000`（改前會算成 `2 天×500 = 1,000`）、
月租／趟次／半天一律擋下、單價 0／空白／文字三種列都被濾掉、
未載明加班費率但有加班時數 → 擋下但同一費率列沒有加班時數時仍正常算 `2×2000 = 4,000`。
