# 維運紀錄（Ops Log）

記錄不涉及程式碼、但影響線上資料/設定的維運操作。真實姓名／廠商等敏感資訊一律不寫入本檔（去識別化政策）。

| 日期 | 操作 | 範圍與方式 |
|---|---|---|
| 2026-07-06 | 移除 3 位工程師人員名單項目 | 依使用者要求，將 3 個不再需要的「工程師人員（people）」名單項目自以下三處同步移除：① 地端 `config.local.js` ② Netlify 環境變數 `LOCAL_CONFIG_JS`（4 個 context 全數）③ 共用資料庫 16 個工地各自的 `people`（read-modify-write，保留各工地其他既有項目與 vendors/lockDate）。本次 commit 之部署會一併重建線上 `config.local.js`，使「新建工地／還原預設」的種子名單同步為最新。點工人員池（workers）未受影響。 |
| 2026-07-28 | 清理人員名單多人並列/誤觸選項 | 依使用者裁示（v15.1 單一人名規則上線配套）：掃描全站發現 3 個工地的 people 池共 12 個含分隔符選項（8 個「甲/乙」式多人並列、1 個帶前綴符號、2 個輸入法誤觸、1 個分號結尾），逐站 read-modify-write 移除；多人並列拆出之人名以單人形式補回名單（缺 3 個補 3 個，其餘原已存在），誤觸垃圾直接捨棄。覆核全站 people 池含分隔符選項＝0。另有 1 筆歷史回報（某工地 7/8 點工單）簽單責任工程師為兩人並列，待工地確認實際代表人後另行修正（未動）。 |
| 2026-07-29 | 各工地名單改為自行建置（保留主力工地） | 依使用者裁示「各工地名單獨立管理、除主力工地外先清空讓各站重建」：①操作前下載完整備份存於工作資料夾（可回復）②保留主力工地（單據量最大者）完整名單不動 ③其餘 10 個工地清空 `vendors`／`people`，但**保留該站既有單據實際引用的名稱**（2 站分別保留 19、3 個；其餘 8 站無單據，完全清空）——避免既有單據因名稱不在清單而無法編輯/回報 ④`locations`／`categories`／`equipTypes`／`laborTypes` 屬泛用詞彙，本次未清空。搭配同日程式改版：新建工地不再帶入分包商與工程師種子名單。 |
| 2026-07-29 | 泛用選項亦改各站自建（僅清空無單據工地） | 依使用者追加裁示「泛用選項也清空，但已有實際填寫回報的工地先不清」：清空 8 個**無任何單據**工地的 `locations`／`categories`／`equipTypes`／`laborTypes`（各 22 個 → 0）；3 個已有單據的工地（單據 212／33／1 筆）維持原名單不動，避免影響進行中的填報。搭配同日程式改版：新建工地六個名單池一律全空、並移除「工種名單為空時自動補預設值」的相容邏輯（否則清空會在載入時被還原）。 |
